services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '127.0.0.1:3002:5173'
    networks:
      - hello-net

networks:
  hello-net:
    driver: bridge
